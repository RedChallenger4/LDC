<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="app.theducksneezes.com.ldc">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".DrawerMenuActivity"
            android:label="@string/title_activity_main_menu"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".GameMenuActivity" />
        <activity android:name=".GoOutsideMenuActivity" />
        <activity android:name=".WatchMoviesMainMenuActivity" />
        <activity android:name=".WhiteboardActivity" />
        <activity android:name=".ConnectMenuActivity" />
        <activity android:name=".DisconnectActivity" />
        <activity android:name=".CalendarActivity" />
        <activity android:name=".StartTimeActivity" />
        <activity android:name=".EndTimeActivity" />
        <activity android:name=".EventDetailsActivity" />
        <activity android:name=".EventListActivity" />
        <activity
            android:name=".ConnectActivity"
            android:label="@string/title_activity_connect"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>
    <uses-permission android:name="android.permission.READ_PHONE_STATE"/>
    <uses-permission android:name="android.permission.CALL_PHONE"/>

</manifest>